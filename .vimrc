"Note: Source .vimrc within VIM rather using bash source. That does not make sense.
" In vim, just type: 
"  :so ~/.vimrc

set shiftwidth=4
set tabstop=4
set ai "Auto indent
set si "Smart indent

"Enable syntax highlighting
syntax enable

set nu
